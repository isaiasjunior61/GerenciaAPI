using GerenciaAPI.src.controllers;
using Microsoft.AspNetCore.Mvc;
using Microsoft.CodeAnalysis.CSharp.Syntax;

namespace GerenciaAPI.src.routes
{
    public static class ProjetoRoutes
    {/*
        public static void RegisterProjetoRoutes(this IEndpointRouteBuilder endpoints)
        {
            endpoints.MapGet("/projetos", (ProjetoController controller) => controller.GetProjeto());
            endpoints.MapPost("/projetos", (ProjetoController controller) => controller.PostProjeto());
            endpoints.MapPut("/projetos/{id}", (ProjetoController controller) => controller.PutProjeto(id));
            endpoints.MapDelete("/projetos/{id}", (ProjetoController controller) => controller.DeleteProjeto(id));

        }*/
    }
}
